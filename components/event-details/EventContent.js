export default function EventContent({ description }) {
  return (
    <section className="relative -top-8 max-w-2xl w-[90%] mx-auto text-black text-center">
      <p>{description}</p>
    </section>
  );
}
